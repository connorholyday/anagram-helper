{
  "name": "anagram",
  "version": "0.1.0",
  "private": true,
  "homepage": "http://connorholyday.github.io/anagram-helper",
  "devDependencies": {
    "react-scripts": "0.8.5"
  },
  "dependencies": {
    "gh-pages": "^0.12.0",
    "react": "^15.4.2",
    "react-dom": "^15.4.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  }
}
